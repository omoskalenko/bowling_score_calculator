/**
 * JSON to JS
 * @param {*} score
 */
export function JSONtoJS(score) {
  return JSON.parse(score)
}

/**
 *  JS to JSON
 * @param {*} score
 */
export function JSToJSON(score) {
  return JSON.stringify(score)
}

